#ifndef BASE_HPP
#define BASE_HPP

    #include "iostream" 
class Base{
    public:
        // Base();
        // Base(const Base &org);
        // Base& operator=(const Base &org);
    public:
        virtual ~Base();
    
};
#endif